<%= form_for(@book) do |f| %>
  <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :book_title %><br>
    <%= f.text_field :book_title %>
  </div>

  <div class="field">
    <%= f.label :authors %><br>
    <%= f.collection_select(:author_ids, @authors, :id, :last_name,
                                    {include_blank: true}, {multiple: true}) %> 
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
